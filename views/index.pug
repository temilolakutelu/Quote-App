extends layout

block content
  h1= title
  p Welcome to #[em QuoteApp], a very basic Express website developed as a test.

  h1 Dynamic content

  if error
    p Error getting dynamic content.
  else
    p The Quote App has the following record counts:

    ul
      li #[strong Quotes:] !{data.quote_count}
     
