extends layout

block content
  h1= title
  
  ul
    each quote in quote_list
      li 
        a(href=quote.url) #{quote.category} 
        |  (#{quote.author.name})

    else
      li There are no quotes.

    